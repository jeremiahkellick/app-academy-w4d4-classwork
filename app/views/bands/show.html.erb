<h1><%= @band.name %></h1>
<h2>Albums</h2>
<ul><%= render @band.albums %></ul>
<p><a href="<%= new_band_album_url(@band) %>">New album</a></p>
<section>
  <a href="<%= bands_url %>">Back</a> |
  <a href="<%= edit_band_url(@band) %>">Edit</a> |
  <%= delete_button name: 'Delete band', action: band_url(@band) %>
</section>
